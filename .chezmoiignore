# Files to ignore (not apply to home directory)
README.md
LICENSE
docs.md
test-docker-linux.sh
bootstrap.sh

# OS-specific ignores
{{- if ne .chezmoi.os "darwin" }}
# Ignore macOS-specific files on non-macOS
Library/**
{{- end }}

{{- if or (ne .chezmoi.os "linux") (not .isWork) }}
# Ignore .bashrc on non-Linux OR on personal machines
# (only manage .bashrc on work Linux cloud workstations)
.bashrc
{{- end }}

{{- if not .isWork }}
# Ignore work-specific files on personal machines
.config/zsh/work.zsh
{{- end }}

# Ignore oh-my-zsh cache
.oh-my-zsh/cache/**

# Files managed by other tools (auto-generated)
.gitconfig-proxy
.metatron/**
.iterm2_shell_integration.zsh
